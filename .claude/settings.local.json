{
  "permissions": {
    "allow": [
      "PowerShell(winget install *)",
      "PowerShell(pip install *)",
      "PowerShell(ruff check *)",
      "Skill(update-config)",
      "PowerShell($json = Get-Content \".claude/settings.json\" -Raw | ConvertFrom-Json; $hooks = $json.hooks.Stop; foreach \\($entry in $hooks\\) { foreach \\($h in $entry.hooks\\) { if \\($h.type -eq \"command\"\\) { Write-Output \"VALIDATED: $\\($h.command\\)\" } } })",
      "Bash(git add *)"
    ]
  }
}
